// Mobile Layout Components
export { MobileLayout, MobileHeader, MobileContent, MobileBottomBar, TouchArea } from '../common/MobileLayout';

// Mobile Interaction Components  
export { GestureHandler, KeyboardHandler, ViewportHandler } from '../common/MobileInteraction';

// Performance Components
export { LazyLoad, LazyImage, VirtualizedList, InfiniteScroll } from '../performance/LazyLoading';

// Mobile Responsive Hooks
export * from '../../hooks/useResponsive';

// Performance Utilities
export * from '../../utilities/performanceUtils';

// Optimized Services
export { optimizedSearchService } from '../../services/optimizedSearchService';
